{
    "description": "Date-time ISO 8601 format including timezone postfix as '+00:00' or simply 'Z'.",
    "type": "string",
    "format": "date-time",
    "comment": "`date-time` enforces ISO 8601 with a timezone postfix as '+00:00' or simply 'Z'."
}